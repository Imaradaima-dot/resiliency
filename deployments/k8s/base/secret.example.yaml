# Template only. Do not commit real secrets.
# Preferred command:
# kubectl create secret generic resiliency-secrets -n resiliency \
#   --from-literal=MONGODB_URI="$MONGODB_URI" \
#   --from-literal=GITHUB_TOKEN="$GITHUB_TOKEN" \
#   --from-literal=OWM_API_KEY="$OWM_API_KEY" \
#   --from-literal=GRAFANA_ADMIN_PASSWORD="replace_with_grafana_admin_password"
apiVersion: v1
kind: Secret
metadata:
  name: resiliency-secrets
  namespace: resiliency
type: Opaque
stringData:
  MONGODB_URI: "REPLACE_WITH_ATLAS_URI"
  GITHUB_TOKEN: "REPLACE_WITH_GITHUB_TOKEN"
  OWM_API_KEY: "REPLACE_WITH_OPENWEATHERMAP_KEY"
  GRAFANA_ADMIN_PASSWORD: "resiliency"
